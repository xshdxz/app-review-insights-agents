# Local research and planning artifacts
.planning/
.superpowers/
tmp/

# Secrets and local configuration
.env
.env.*
!.env.example
# Streamlit 本地 secrets（Cloud 上用 Secrets UI，本地可能有人建这个文件）
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
# 测试把 MagicMock 当路径用过 → 会在仓库根生成真实目录（D-21，误提交过一次），这里兜底
MagicMock/
.venv/
venv/

# Runtime data and generated outputs
data/runs/
data/uploads/
data/agent/
output/
# 模型响应缓存（可随时重建的派生数据）。注意 data/cache/ 里还有**被跟踪**的演示档案
# demo-run.json，所以只忽略库文件，不忽略整个目录。
data/cache/*.sqlite3*

# Editors and OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Coverage artifacts
.coverage
.coverage.*
