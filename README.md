# App Review Insights

一个将美国区 App Store 评论转化为产品发现、PRD 需求和可追溯测试用例的中文分析工具。

当前处于早期开发阶段，完整安装、使用和架构说明将在功能完成后补充。

> 兼容性说明：`app-store-scraper` 固定依赖过旧的 `requests`，与当前
> Streamlit 冲突。项目将通过独立采集适配器使用兼容的 HTTP 后端，避免该冲突。
