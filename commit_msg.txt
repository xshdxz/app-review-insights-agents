fix: 混合检索改成两路召回（D-19）——此前只是 FTS 的重排器

- 现象：CorpusRetriever._rank 只对 **FTS 已召回的候选**做融合（for chunk in chunks），
  向量那一路找到的评论根本没有机会进入结果。这样的「混合」**结构上不可能提升召回**，
  只能改动 FTS 命中的顺序，与 README 的表述所暗示的能力不符
- 来源：T6 造检索评测时读代码发现；search_embeddings 其实已经返回 content/app_id/platform，
  构造候选所需的数据全在手上，只是没被使用
- 改法：向量那一路的候选（**先过平台过滤**，社交语料不能从侧门溜进来）并入候选集，
  FTS 分为 0、靠向量分竞争；融合权重与截断逻辑不变
- 新增两项测试：只有向量能找到的评论必须进入结果；向量补进来的候选同样受平台隔离约束
- 评测脚本补 --hybrid：按 Settings 造向量后端（本地模型优先，其次 API），
  造不出来时**如实记进 skipped**（"EMBEDDING_ENABLED=false…"），不拿假后端充数
- 向量那一路的**质量仍未测**：本机确认无 provider（DeepSeek 不提供 /embeddings，
  没有本地向量运行时，HF 缓存里只有 reranker）。接线已就绪，配好后一条命令出数字
- 全量 689 项全绿，ruff 双绿
